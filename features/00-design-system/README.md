# Feature: Design system

Slice 00. Tokens, primitives, app shell and a living component gallery, so no later
feature has to invent its own visual language — and so progress stays visible from the
first week instead of appearing only when a feature is finished.

Depends on: 01 (workspace only). Runs in parallel with everything.
Feeds: 04, 05, 06, 08, 11, 12, 13.
Visual reference: the design canvas linked in `docs/design.md`.
