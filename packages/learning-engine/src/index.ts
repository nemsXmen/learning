/**
 * Pure pedagogical decisions: mastery, forgetting risk, review priority,
 * recommendations, XP, streak, boost plans.
 *
 * No I/O, no clock, no framework types — the clock is always an input.
 * Implemented by features/09-learning-engine-core; empty until that slice runs.
 */
export {};
